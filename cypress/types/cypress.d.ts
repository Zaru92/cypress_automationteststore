declare global {
  namespace Cypress {
    interface Chainable {
      /**
       * Placeholder for future custom commands.
       * We will extend this interface step by step.
       */
    }
  }
}

export {};
